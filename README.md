# nvim

Clone my config here

```console
~/.config/nvim
```

## Dependencies

```sh
brew install fzf
brew install ripgrep
brew instlal lazygit
brew install node
brew install fd
brew install --cask font-hack-nerd-font
brew install luarocks &&   luarocks install luacheck
brew install shellcheck
```
